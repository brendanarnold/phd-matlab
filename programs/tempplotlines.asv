function tempplotlines(r0,d,points);

figure;
hold on;
lamda=-5:0.1:5;
for planenum=1:size(r0,1)
    xs=r0(planenum,1)+lamda*d(planenum,1);
    ys=r0(planenum,2)+lamda*d(planenum,2);
    zs=r0(planenum,3)+lamda*d(planenum,3);
    plot3(xs,ys,zs);
end
fot
plot3(